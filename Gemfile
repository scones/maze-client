source 'https://rubygems.org'


gem 'rails', '4.2.6'                  # obvious
gem 'closure-compiler'                # smaller javascript for faster page load
gem 'sass-rails'                      # allways useful
gem 'unicorn'                         # server
gem 'high_voltage'                    # disable dynamic rendering
gem 'haml'                            # less to write

group :development, :test do
  gem 'byebug'                     # debugger
end

group :development do
end

